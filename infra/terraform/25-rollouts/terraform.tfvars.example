# Copy to terraform.tfvars (git-ignored) and edit. Every variable has a default,
# so this file is only needed to override one.
# argo_rollouts_chart_version = "2.43.0"
# controller_replicas         = 1
# dashboard_enabled           = false
