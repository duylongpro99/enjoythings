version: "2"

linters:
  default: standard
  exclusions:
    generated: lax
    paths:
      - gen

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
    paths:
      - gen
